// @qstd/crypto — the keystone SchemeRegistry: seal/open/sign/verify/break for every
// scheme in PLAN §5 (plaintext, sha256, hmac-sha256, rsa-oaep, ecdh-aes, hybrid-mlkem,
// ecdsa-p256, ml-dsa-65). Built and tested in M2; this placeholder keeps the package
// importable. Crypto correctness is non-negotiable — see CLAUDE.md.

export const PACKAGE_NAME = "@qstd/crypto" as const;
