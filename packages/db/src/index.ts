// @qstd/db — Drizzle schema, migrations, seed and the Neon client (PLAN §4). The schema
// (crypto_config, trades, mappings, wire_messages, harvested_packets, audit_log) plus the
// Better Auth tables land in M1. This placeholder keeps the package importable.

export const PACKAGE_NAME = "@qstd/db" as const;
